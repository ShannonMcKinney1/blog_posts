name = "blog_posts"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "./dist"

# Environment variables for build
[vars]
NODE_VERSION = "20.11.0"

# KV namespace for sessions (local development)
[[kv_namespaces]]
binding = "SESSION"
id = "local-session-namespace"
preview_id = "local-session-namespace"
